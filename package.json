{
  "name": "fhir-server-proxy-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild *.ts --bundle --minify --sourcemap --platform=node --outdir=dist/",
    "postbuild": "zip -r ~/Desktop/index.zip *",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/node": "^20.12.7",
    "aws-sdk": "^2.1604.0",
    "axios": "^1.6.8",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
